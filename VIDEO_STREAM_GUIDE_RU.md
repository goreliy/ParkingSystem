# 📹 Руководство по видеопотоку в браузере

## Что добавлено

Теперь в системе есть **живой видеопоток** прямо в браузере вместо статичных снимков!

### Возможности:

1. **📊 Панель управления** - видео с разметкой парковочных мест в реальном времени
2. **⚙️ Настройка** - живой поток для удобной разметки мест (можно кликать по видео!)
3. **🔄 Переключение** - выбор между видео и статичными снимками

---

## Как использовать

### В Панели управления (Dashboard)

1. Откройте **Панель управления** (`/dashboard`)
2. Выберите парковочную зону
3. В правой панели найдите чекбокс **"Видеопоток в реальном времени"**
4. **Включите** его
5. Вместо статичного изображения появится живое видео (~10 FPS)
6. Вся разметка парковочных мест отображается в реальном времени!

**Преимущества:**
- ✅ Видите изменения сразу
- ✅ Видео обновляется автоматически
- ✅ Видны машины въезжающие/выезжающие
- ✅ Разметка рисуется на каждом кадре

**Экономия трафика:**
- Выключите видеопоток → будут статичные снимки каждые 10 секунд

### В Настройке (Setup)

1. Откройте **Настройка** (`/setup`)
2. Выберите зону для настройки парковочных мест
3. В секции "Вид с камеры" включите **"Видеопоток"**
4. Появится живое видео с камеры
5. **Можно кликать прямо по видео** для разметки координат!

**Удобство для разметки:**
- ✅ Видите машины в движении
- ✅ Можете дождаться когда машина встанет ровно
- ✅ Кликаете по живому видео как по статичному изображению
- ✅ Координаты определяются корректно

---

## Технические детали

### Формат потока: MJPEG

```
GET /api/video/camera/<camera_id>    - чистое видео с камеры
GET /api/video/space/<space_id>      - видео с разметкой парковочных мест
```

### Параметры:
- **FPS**: ~10 кадров в секунду
- **Качество JPEG**: 75% (баланс качества/трафика)
- **Задержка**: ~100-200 мс

### Расход трафика:

Примерный расход на **одну камеру**:

| Режим | Трафик в секунду | Трафик в минуту | Трафик в час |
|-------|------------------|-----------------|--------------|
| Статичный (10 сек) | ~5 KB | ~30 KB | ~1.8 MB |
| Видеопоток (10 FPS) | ~300 KB | ~18 MB | ~1 GB |

**Рекомендация**: 
- Используйте видеопоток для **активной работы** (настройка, мониторинг)
- Переключайтесь на статичный для **фонового мониторинга**

---

## Решение проблем с YOLO моделями

### Проблема: "Failed to load model" с PyTorch 2.6

**Причина**: PyTorch 2.6 изменил политику безопасности загрузки моделей

**✅ Исправлено в коде!** 

Теперь система автоматически:
1. Определяет версию PyTorch
2. Если 2.6+ → использует `weights_only=False` для доверенных YOLO моделей
3. Добавляет YOLO классы в безопасные глобальные

**Действия:** Просто перезапустите приложение.

### Быстрое решение "Не находит машины"

```bash
# 1. Перезапустите приложение
python backend/app.py --host 0.0.0.0 --port 5000

# 2. Откройте Config
http://localhost:5000/config

# 3. Прокрутите до "🤖 Управление YOLO моделями"

# 4. Загрузите yolo11s.pt (РЕКОМЕНДУЕТСЯ - новая улучшенная модель)
#    Нажмите "Загрузить" → подождите 1-2 минуты

# 5. Активируйте yolo11s.pt
#    Нажмите "Активировать"

# 6. Тестируйте детекцию
#    Выберите камеру → "Тестировать детекцию"

# 7. Должно показать найденные машины!
```

---

## Доступные YOLO модели (обновлено!)

### YOLOv8 (стандартные):
- yolov8n.pt - 6 MB - самая быстрая
- yolov8s.pt - 22 MB - хорошая
- yolov8m.pt - 52 MB - отличная
- yolov8l.pt - 88 MB - очень хорошая
- yolov8x.pt - 138 MB - максимальная

### YOLOv11 (НОВЫЕ! Улучшенные):
- **yolo11n.pt** - 5 MB - очень быстрая, хорошая точность
- **yolo11s.pt** - 19 MB - **РЕКОМЕНДУЕТСЯ** ✅ - лучший баланс
- **yolo11m.pt** - 40 MB - отличная точность
- **yolo11l.pt** - 50 MB - превосходная
- **yolo11x.pt** - 110 MB - максимальная

**Почему YOLOv11 лучше:**
- 🚀 Быстрее чем v8 при той же точности
- 🎯 Точнее находит машины
- 💾 Меньше размер файлов
- ⚡ Меньше нагрузка на CPU/GPU

**Рекомендация**: Используйте **yolo11s.pt** вместо yolov8s.pt!

---

## Пошаговая инструкция

### Первый запуск с YOLOv11:

1. **Перезапустите приложение**:
   ```powershell
   python backend/app.py --host 0.0.0.0 --port 5000
   ```

2. **Откройте Config**:
   ```
   http://localhost:5000/config
   ```

3. **Загрузите yolo11s.pt**:
   - Найдите в таблице "YOLOv11 Small" (помечено "Рекомендуется")
   - Нажмите "Загрузить"
   - Подождите ~1 минуту (размер 19 MB)

4. **Активируйте модель**:
   - Нажмите "Активировать" для yolo11s.pt
   - Подождите 5-10 секунд (загрузка в память)
   - Увидите сообщение "Модель активирована!"

5. **Проверьте детекцию**:
   - Выберите камеру "двор" или "школа"
   - Нажмите "Тестировать детекцию"
   - Должно показать: "✅ Найдено транспортных средств: N"

6. **Проверьте видеопоток**:
   - Перейдите в Панель управления
   - Выберите зону
   - Включите "Видеопоток в реальном времени"
   - Увидите живое видео!

---

## Видеопоток + Разметка

Когда включен видеопоток с разметкой, вы видите:

- 🟢 **Зеленые зоны** - свободные парковочные места
- 🔴 **Красные зоны** - занятые места
- 🔵 **Синие зоны** - зоны запрета парковки
- 📝 **Метки** - номера мест (A1, A2, #5, #12...)
- 🎨 **Полупрозрачная заливка** - не закрывает видео
- 📊 **Обновление в реальном времени** - статус меняется на лету

---

## API для видеопотока

```bash
# Видео с камеры (без разметки)
http://localhost:5000/api/video/camera/cam_xxx

# Видео зоны (с разметкой парковочных мест)
http://localhost:5000/api/video/space/space_xxx

# Можно открыть прямо в браузере или использовать в <img>:
<img src="http://localhost:5000/api/video/space/space_abc123">
```

---

## Сравнение: Видеопоток vs Статичные снимки

### Видеопоток (MJPEG):
- ✅ Реальное время (~10 FPS)
- ✅ Видно движение машин
- ✅ Удобно для настройки и разметки
- ❌ Больше трафика (~300 KB/сек на камеру)
- ❌ Больше нагрузка на сервер

### Статичные снимки:
- ✅ Минимальный трафик (~5 KB/сек)
- ✅ Минимальная нагрузка
- ❌ Обновление каждые 10 секунд
- ❌ Не видно промежуточных состояний

**Лучшая практика:**
- **Видео** - для активной работы, настройки, демонстрации
- **Статика** - для фонового мониторинга, экономии ресурсов

---

## Итоги обновления

### ✅ Что работает сейчас:

1. **10 YOLO моделей** - v8 и v11, от nano до xlarge
2. **Управление моделями** - загрузка, активация, удаление через UI
3. **Тест детекции** - проверка работы модели на камере
4. **Видеопоток в Dashboard** - с разметкой парковочных мест
5. **Видеопоток в Setup** - для удобной разметки
6. **Переключение режимов** - видео/статика одной галочкой
7. **Исправлена совместимость** с PyTorch 2.6

### 🎯 Рекомендации:

**Для начала работы:**
1. Загрузите **yolo11s.pt** (19 MB, быстрая и точная)
2. Активируйте её
3. Протестируйте детекцию
4. Включите видеопоток для комфортной работы

**Если нужна максимальная точность:**
- Используйте **yolo11m.pt** или **yolo11l.pt**

**Если много камер и слабый компьютер:**
- Используйте **yolo11n.pt** (самая быстрая)
- Отключайте видеопоток когда не используете

---

Система полностью готова! 🚀

