# Инструкция по настройке стриминга в Telegram

## Требования

- Telegram Premium или Business аккаунт (для создания видеочата/прямого эфира)
- FFmpeg установлен на сервере
- Бот добавлен в группу как администратор

## Шаг 1: Создание группы в Telegram

1. Откройте Telegram и создайте новую группу
2. Добавьте участников (если необходимо)
3. **Важно**: Добавьте бота в группу как администратора
   - Откройте настройки группы
   - Перейдите в "Администраторы"
   - Добавьте бота и дайте ему права администратора

## Шаг 2: Начало видеочата/прямого эфира

1. В группе нажмите на кнопку "Видеозвонок" или "Прямой эфир"
2. Начните видеочат или прямой эфир
3. Telegram предоставит вам RTMP данные:
   - **RTMP URL**: `rtmp://...` (например, `rtmp://dc5-1.rtmp.t.me/s/...`)
   - **Stream Key**: длинная строка символов

**Важно**: Сохраните эти данные, они понадобятся для настройки.

## Шаг 3: Получение Chat ID группы

Для получения Chat ID группы:

1. Добавьте в группу бота [@userinfobot](https://t.me/userinfobot)
2. Бот покажет ID группы (отрицательное число, например `-1001234567890`)
3. Или используйте [@getidsbot](https://t.me/getidsbot)

**Примечание**: Chat ID для групп всегда отрицательный.

## Шаг 4: Настройка в Web UI

1. Откройте веб-интерфейс системы (обычно `http://localhost:5000`)
2. Перейдите в раздел **Configuration** → **Streaming Configuration**
3. Нажмите **"Add Target"** (Добавить цель)
4. Заполните форму:
   - **Alias** (Псевдоним): например, `main_group` или `group_1`
   - **Chat ID**: ID группы (отрицательное число, например `-1001234567890`)
   - **Title** (Название): например, "Основная группа"
   - **RTMP URL**: URL, полученный от Telegram (без stream key)
   - **Stream Key**: ключ стрима, полученный от Telegram
5. Нажмите **"Save"** (Сохранить)

## Шаг 5: Запуск стрима через бота

### Для пользователей (в группе):

1. Откройте группу с ботом
2. Отправьте команду `/start1` боту
3. Нажмите кнопку **"▶️ Запустить стрим"**
4. Выберите камеру из списка (inline кнопки)
5. Стрим начнется автоматически в текущую группу

### Для администраторов:

1. Можно использовать команду: `/start_stream <camera_id> [target_alias]`
2. Или использовать кнопку **"▶️ Запустить стрим"** в клавиатуре

## Остановка стрима

1. Нажмите кнопку **"⏹️ Остановить стрим"** в клавиатуре
2. Или используйте команду `/stop_stream` (для админов)

## Проверка статуса стрима

1. Нажмите кнопку **"📊 Статус стрима"**
2. Или используйте команду `/stream_status`

## Важные ограничения

- **Только один стрим** может быть активен одновременно
- Если стрим уже запущен, нужно сначала остановить его
- Стрим можно запускать только в группах, не в личных чатах
- Для каждой группы нужно настроить отдельный target в Web UI

## Устранение проблем

### Стрим не запускается

1. Проверьте, что FFmpeg установлен:
   ```bash
   ffmpeg -version
   ```

2. Проверьте, что target настроен правильно в Web UI
3. Проверьте, что Chat ID группы указан правильно (отрицательное число)
4. Проверьте логи: `logs/parking.log`

### Стрим запускается, но не виден в группе

1. Убедитесь, что видеочат/прямой эфир активен в группе
2. Проверьте, что RTMP URL и Stream Key актуальны (они могут меняться)
3. Проверьте, что бот имеет права администратора в группе

### Ошибка "Target не настроен"

1. Убедитесь, что target добавлен в Web UI с правильным Chat ID
2. Chat ID должен точно совпадать с ID группы (включая знак минус)
3. Перезапустите приложение после добавления target

## Дополнительная информация

- Стрим использует FFmpeg для передачи видео с камеры в Telegram
- Видео передается без перекодирования (если возможно) для экономии ресурсов
- Стрим автоматически останавливается при падении процесса FFmpeg
- Watchdog следит за процессом и очищает состояние при ошибках

