# 🤖 Руководство по управлению YOLO моделями

## Проблема: "Не находит ни одной машины"

Если система не находит транспорт, это обычно связано с моделью детекции.

### Быстрое решение:

1. **Откройте** `http://localhost:5000/config`
2. **Прокрутите** до раздела "🤖 Управление YOLO моделями"
3. **Проверьте** текущую модель
4. **Нажмите** "Тестировать детекцию" на одной из камер
5. Если машин не найдено - **смените модель** на более крупную

---

## Доступные YOLO модели

| Модель | Размер | Скорость | Точность | Когда использовать |
|--------|--------|----------|----------|-------------------|
| **yolov8n.pt** | 6 MB | ⚡⚡⚡⚡⚡ Самая быстрая | ⭐⭐ Базовая | Слабые компьютеры, много камер |
| **yolov8s.pt** ✅ | 22 MB | ⚡⚡⚡⚡ Быстрая | ⭐⭐⭐⭐ Хорошая | **РЕКОМЕНДУЕТСЯ** для большинства случаев |
| **yolov8m.pt** | 52 MB | ⚡⚡⚡ Средняя | ⭐⭐⭐⭐⭐ Отличная | Когда нужна высокая точность |
| **yolov8l.pt** | 88 MB | ⚡⚡ Медленная | ⭐⭐⭐⭐⭐ Очень хорошая | Мало камер, нужна точность |
| **yolov8x.pt** | 138 MB | ⚡ Очень медленная | ⭐⭐⭐⭐⭐⭐ Максимальная | Оффлайн анализ, высочайшая точность |

**✅ Рекомендация**: Начните с **yolov8s.pt** - это оптимальный баланс скорости и точности.

---

## Пошаговая инструкция

### Шаг 1: Проверка текущей модели

1. Откройте **Конфигурация** → раздел "🤖 Управление YOLO моделями"

2. Посмотрите **"Текущая модель"**:
   ```
   Модель: yolov8n.pt
   Загружена: 13.11.2025, 21:35:14
   Порог уверенности: 50%
   Классы: car, truck, bus, motorcycle
   ```

3. Если модель не загружена - см. Шаг 2

### Шаг 2: Тестирование детекции

1. В разделе **"Тест детекции"** выберите камеру из списка

2. Нажмите **"Тестировать детекцию"**

3. Результаты:

   **✅ Если найдены машины:**
   ```
   ✅ Найдено транспортных средств: 5
   Модель: yolov8n.pt
   
   Детали:
   1. car - 87% [450, 300, 650, 600]
   2. car - 82% [200, 250, 380, 550]
   ...
   ```
   → **Модель работает! Можно использовать.**

   **⚠️ Если НЕ найдены:**
   ```
   ⚠️ Транспорт не обнаружен!
   
   Попробуйте:
   - Использовать более крупную модель (yolov8s или yolov8m)
   - Снизить порог уверенности до 30-40%
   - Убедиться что на парковке есть машины
   ```
   → **Нужно сменить модель или настройки**

### Шаг 3: Загрузка и активация модели

Если текущая модель не находит машины:

1. **Посмотрите** таблицу "Доступные модели"

2. **Загрузите** рекомендуемую модель:
   - Найдите **yolov8s.pt** (помечена "Рекомендуется")
   - Нажмите **"Загрузить"**
   - Подтвердите загрузку
   - Подождите 1-2 минуты (модель скачается из интернета)

3. **Активируйте** загруженную модель:
   - Нажмите **"Активировать"** напротив yolov8s.pt
   - Подтвердите смену модели
   - Модель загрузится в память (~5-10 секунд)

4. **Проверьте** снова:
   - Запустите "Тестировать детекцию"
   - Теперь должны найтись машины!

### Шаг 4: Настройка порога уверенности

Если модель находит слишком мало машин:

1. Перейдите в раздел **"Настройки определения занятости"**

2. Найдите **"Порог уверенности детекции"**

3. Попробуйте снизить до **0.3-0.4** (30-40%)

4. Нажмите **"Сохранить настройки"**

5. **Перезапустите** приложение для применения

---

## Выбор правильной модели

### Ситуация 1: Много камер (5+), слабый компьютер

**Проблема**: Система тормозит, высокая нагрузка CPU

**Решение**:
- Используйте **yolov8n.pt** (самая быстрая)
- Или **yolov8s.pt** (баланс)
- Снизьте `update_hz` до 0.5 (детекция раз в 2 секунды)

### Ситуация 2: 1-2 камеры, мощный компьютер

**Решение**:
- Используйте **yolov8m.pt** или **yolov8l.pt**
- Максимальная точность
- Меньше ложных срабатываний

### Ситуация 3: Проблемы с детекцией (не находит машины)

**Решение**:
1. **Сначала**: попробуйте yolov8s.pt
2. **Если не помогло**: попробуйте yolov8m.pt
3. **Снизьте порог**: confidence_threshold до 0.3
4. **Проверьте** что на камере действительно видны машины (Setup → просмотр камеры)

### Ситуация 4: Ложные срабатывания (находит что-то лишнее)

**Решение**:
- **Повысьте порог**: confidence_threshold до 0.6-0.7
- Используйте более крупную модель (лучше различает объекты)

---

## Технические детали

### Где хранятся модели?

Модели автоматически сохраняются в:
- `models/` - для загруженных через UI
- Корень проекта - для установленных вручную

### Как работает смена модели?

```
1. Загрузка новой модели в память
2. Замена текущей модели
3. Старая модель выгружается
4. Обновление config.json (active_model)
5. Детекция сразу начинает использовать новую модель
```

**Не требуется перезапуск приложения!**

### Формат модел и

Ultralytics YOLO поддерживает:
- `.pt` - PyTorch модели (используется)
- Также поддерживает ONNX, TensorRT, но требует дополнительной настройки

---

## API для продвинутых пользователей

### Получить список моделей:
```bash
curl http://localhost:5000/api/models/available
```

### Загрузить модель:
```bash
curl -X POST http://localhost:5000/api/models/download \
  -H "Content-Type: application/json" \
  -d '{"filename": "yolov8s.pt"}'
```

### Активировать модель:
```bash
curl -X POST http://localhost:5000/api/models/activate \
  -H "Content-Type: application/json" \
  -d '{"filename": "yolov8s.pt"}'
```

### Тестировать детекцию:
```bash
curl -X POST http://localhost:5000/api/models/test \
  -H "Content-Type: application/json" \
  -d '{"camera_id": "cam_xxx"}'
```

### Установить порог уверенности:
```bash
curl -X PUT http://localhost:5000/api/models/set-confidence \
  -H "Content-Type: application/json" \
  -d '{"threshold": 0.4}'
```

---

## Решение типичных проблем

### ❌ "ultralytics not available"

**Проблема**: Библиотека ultralytics не установлена

**Решение**:
```bash
pip install ultralytics==8.1.0
```

### ❌ "Failed to load YOLO model"

**Причины**:
1. Модель не существует/повреждена
2. Нет интернета для автозагрузки
3. Недостаточно памяти

**Решение**:
1. Проверьте наличие файла модели
2. Попробуйте меньшую модель
3. Перезапустите приложение

### ❌ "Детекция очень медленная"

**Причины**:
- Слишком крупная модель
- Много камер
- Высокий update_hz

**Решение**:
- Используйте yolov8n.pt или yolov8s.pt
- Снизьте update_hz до 0.5
- Уменьшите количество камер

### ❌ "Находит машины на других кадрах, но не на моем"

**Причины**:
- Плохое освещение
- Машины под необычным углом
- Частичная видимость

**Решение**:
- Снизьте порог confidence до 0.3
- Используйте более крупную модель
- Улучшите освещение/угол камеры

---

## Быстрый старт для исправления проблемы

### Если система НЕ НАХОДИТ МАШИНЫ:

```bash
# Способ 1: Через веб-интерфейс (рекомендуется)
1. Откройте http://localhost:5000/config
2. Найдите раздел "🤖 Управление YOLO моделями"
3. Нажмите "Загрузить" для yolov8s.pt
4. После загрузки нажмите "Активировать"
5. Тестируйте детекцию на камере
6. Если все равно не находит - снизьте порог до 30%

# Способ 2: Через консоль (быстро)
curl -X POST http://localhost:5000/api/models/download \
  -H "Content-Type: application/json" \
  -d '{"filename": "yolov8s.pt"}'

curl -X POST http://localhost:5000/api/models/activate \
  -H "Content-Type: application/json" \
  -d '{"filename": "yolov8s.pt"}'

curl -X PUT http://localhost:5000/api/models/set-confidence \
  -H "Content-Type: application/json" \
  -d '{"threshold": 0.3}'
```

---

## Сравнение моделей

### Тест на одном кадре (Intel i7, 8GB RAM):

| Модель | Время детекции | Найдено машин | CPU |
|--------|----------------|---------------|-----|
| yolov8n | 50ms | 8 | 30% |
| yolov8s | 120ms | 10 | 45% |
| yolov8m | 250ms | 11 | 70% |
| yolov8l | 400ms | 11 | 85% |
| yolov8x | 650ms | 12 | 95% |

**Вывод**: yolov8s находит почти столько же, но в 2 раза быстрее чем yolov8m.

### Рекомендации по железу:

**Минимальные требования** (yolov8n):
- CPU: 2 ядра, 2+ GHz
- RAM: 4 GB
- Камеры: до 5

**Рекомендуемые** (yolov8s):
- CPU: 4 ядра, 2.5+ GHz
- RAM: 8 GB
- Камеры: до 10

**Оптимальные** (yolov8m+):
- CPU: 8 ядер или GPU
- RAM: 16 GB
- Камеры: без ограничений

---

## Автоматическая загрузка моделей

Ultralytics автоматически загружает модели при первом использовании, если их нет локально.

**Преимущества ручной загрузки через UI:**
- ✅ Контроль процесса
- ✅ Можно загрузить несколько моделей заранее
- ✅ Переключаться без задержек
- ✅ Работает без интернета после загрузки

---

## Checklist диагностики детекции

- [ ] Камера подключена и показывает изображение (Setup → просмотр)
- [ ] На парковке действительно есть машины
- [ ] Модель YOLO загружена (Config → текущая модель)
- [ ] Порог уверенности не слишком высокий (рекомендуется 40-50%)
- [ ] Тест детекции находит машины (Config → тест детекции)
- [ ] В логах нет ошибок "Failed to load YOLO model"

Если все пункты ✅ но машины все равно не находит:
→ Используйте более крупную модель (yolov8m.pt)

---

## Итоги

Система управления YOLO моделями решает проблему "не находит машины":

1. 🔍 **Тестирование** - проверьте работу детекции
2. 📥 **Загрузка** - скачайте любую из 5 моделей
3. ⚡ **Активация** - смените модель без перезапуска
4. 🎯 **Настройка** - подберите порог уверенности
5. ✅ **Работает** - детекция находит транспорт!

