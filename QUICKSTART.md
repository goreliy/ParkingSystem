# Быстрый старт - Parking Monitor

## Установка за 5 минут

### Windows

1. **Скачайте проект**
   ```powershell
   # Распакуйте в D:\parking-monitor
   cd D:\parking-monitor
   ```

2. **Запустите**
   ```powershell
   .\run_windows.ps1
   ```

3. **Откройте браузер**
   ```
   http://localhost:5000
   ```

### Linux

1. **Скачайте проект**
   ```bash
   cd /opt/parking-monitor
   ```

2. **Установите FFmpeg**
   ```bash
   sudo apt-get install ffmpeg  # Ubuntu/Debian
   ```

3. **Запустите**
   ```bash
   chmod +x run_linux.sh
   ./run_linux.sh
   ```

4. **Откройте браузер**
   ```
   http://localhost:5000
   ```

## Первоначальная настройка

### Шаг 1: Добавьте камеру

1. Перейдите в **Setup** → Camera Management
2. Нажмите "Add Camera"
3. Введите:
   - **Name**: Main Entrance
   - **RTSP URL**: `rtsp://admin:password@192.168.1.100:554/stream`
4. Нажмите "Create"

**Тестирование RTSP URL:**
```bash
# Проверьте URL в VLC
vlc rtsp://admin:password@192.168.1.100:554/stream
```

### Шаг 2: Создайте парковочное пространство

1. В разделе **Parking Spaces** нажмите "Add Space"
2. Введите название: "Ground Floor"
3. Нажмите "Assign" и выберите камеру

### Шаг 3: Разметьте парковочные места

1. В разделе **Parking Spot Configuration** выберите созданное пространство
2. Кликните на изображение два раза для выбора прямоугольника
3. Заполните:
   - **Type**: Parking
   - **Label**: A1
   - **Coordinates**: (автоматически заполнятся при кликах)
4. Нажмите "Add Spot"
5. Повторите для всех мест

### Шаг 4: Настройте Telegram бота

1. Создайте бота через [@BotFather](https://t.me/BotFather)
   ```
   /newbot
   My Parking Bot
   my_parking_bot
   ```
   
2. Скопируйте токен

3. В Web UI перейдите в **Configuration**

4. Вставьте токен и нажмите "Save Token"

5. Отправьте `/start` вашему боту

6. В Web UI установите галочку "Admin" для вашего чата

### Шаг 5: Проверьте работу

1. Перейдите в **Dashboard**
2. Вы должны увидеть:
   - Список пространств
   - Статистику по местам
   - Изображение с разметкой

3. В Telegram отправьте:
   ```
   /spaces
   /image_space <space_id>
   ```

## Настройка детекции

### Порог занятости

По умолчанию место считается занятым, если транспорт стоит **5 минут**.

Изменить можно в **Configuration** → Occupancy Detection Settings:
- **Occupancy Threshold**: 1-60 минут

Или через Telegram:
```
/set_occupancy_minutes 3
```

### Точность детекции

В **Configuration** → Occupancy Detection Settings:
- **Confidence Threshold**: 0.3-0.9 (по умолчанию 0.5)
  - Меньше = больше детекций, но больше ложных
  - Больше = меньше детекций, но точнее

### Частота проверки

- **Update Frequency**: 0.1-10 Hz (по умолчанию 1.0)
  - 1.0 Hz = 1 проверка в секунду
  - 0.5 Hz = 1 проверка в 2 секунды

## Настройка стриминга в Telegram

### Требования
- Telegram Premium или Business аккаунт
- FFmpeg установлен

### Шаг 1: Создайте группу
1. Создайте группу в Telegram
2. Добавьте бота как администратора
3. Начните видеочат/прямой эфир

### Шаг 2: Получите RTMP данные
Telegram предоставит:
- **RTMP URL**: `rtmp://...`
- **Stream Key**: `...`

### Шаг 3: Настройте в Web UI
1. **Configuration** → Streaming Configuration
2. Нажмите "Add Target"
3. Заполните:
   - **Alias**: main_group
   - **Chat ID**: -1001234567890 (ID группы)
   - **Title**: Main Group
   - **RTMP URL**: (из Telegram)
   - **Stream Key**: (из Telegram)
4. Сохраните

### Шаг 4: Запустите стрим
В Telegram:
```
/start_stream <camera_id> main_group
```

Или через Web UI в разделе Configuration.

Остановка:
```
/stop_stream
```

## Типичные проблемы

### Камера не подключается
```bash
# Проверьте URL в VLC
vlc rtsp://your-url

# Проверьте сетевую доступность
ping 192.168.1.100

# Проверьте логи
cat logs/parking.log | grep "camera_id"
```

### YOLO не детектирует
- Проверьте освещение на изображении
- Уменьшите `confidence_threshold` до 0.3
- Убедитесь, что транспорт в кадре
- Проверьте, что места правильно размечены

### Бот не отвечает
```bash
# Проверьте токен
curl https://api.telegram.org/bot<YOUR_TOKEN>/getMe

# Проверьте логи
grep "bot" logs/parking.log

# Перезапустите приложение
```

### FFmpeg не найден
```bash
# Windows
where ffmpeg

# Linux
which ffmpeg

# Установка (Ubuntu)
sudo apt-get install ffmpeg
```

## Команды Telegram бота

### Пользователь
```
/help          - Справка
/spaces        - Список пространств
/space <id>    - Детали пространства
/image_camera <id>  - Снимок с камеры
/image_space <id>   - Снимок пространства с разметкой
```

### Администратор
```
/add_camera <name> <url>                    - Добавить камеру
/list_cameras                                - Список камер
/add_space <name>                           - Создать пространство
/assign_camera <space_id> <camera_id>      - Назначить камеру
/add_spot <space_id> parking A1 100,100;200,200  - Добавить место
/delete_spot <spot_id>                      - Удалить место
/set_occupancy_minutes 3                    - Порог 3 минуты
/start_stream <camera_id>                   - Запустить стрим
/stop_stream                                 - Остановить стрим
/stream_status                               - Статус стрима
```

## API примеры

### Получить статус всех пространств
```bash
curl http://localhost:5000/api/state
```

### Добавить камеру
```bash
curl -X POST http://localhost:5000/api/cameras \
  -H "Content-Type: application/json" \
  -d '{
    "name": "Camera 1",
    "rtsp_url": "rtsp://admin:pass@192.168.1.100/stream"
  }'
```

### Получить снимок с камеры
```bash
curl http://localhost:5000/api/snapshot/camera/cam_12345678 > snapshot.jpg
```

### Real-time обновления (SSE)
```javascript
const eventSource = new EventSource('/api/events');
eventSource.onmessage = (event) => {
  const data = JSON.parse(event.data);
  console.log('Update:', data);
};
```

## Поддержка

**Логи:**
```bash
# Windows
type logs\parking.log

# Linux
tail -f logs/parking.log
```

**Остановка:**
```
Ctrl+C
```

**Перезапуск:**
- Просто запустите скрипт снова
- Или `systemctl restart parking` (Linux systemd)

## Следующие шаги

1. ✅ Система запущена
2. ✅ Камеры добавлены
3. ✅ Места размечены
4. ✅ Бот настроен
5. 📊 Мониторьте через Dashboard или Telegram
6. 🎥 Настройте стриминг (опционально)

---

**Дополнительная информация:** см. [README.md](README.md)

